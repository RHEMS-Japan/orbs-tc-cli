description: >
  This command echos "Hello World" using file inclusion.
steps:
  - run:
      name: setup python
      command: << include(scripts/setup-python.sh) >>
  - run:
      name: install tccli
      command: pip3 install tccli-intl-en
